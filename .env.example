# 应用配置模板：复制为 .env 后按需修改（.env 已加入 .gitignore，禁止提交真实密钥）
APP_NAME=ClipShare
ENVIRONMENT=development
LOG_LEVEL=INFO
# 本机直连数据库时使用；docker compose 环境内由 compose 文件自动注入
DATABASE_URL=postgresql+psycopg://clipshare:clipshare@localhost:5432/clipshare

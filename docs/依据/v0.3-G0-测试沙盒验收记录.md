# v0.3-G0 测试沙盒验收记录

> 验收日期：2026-08-23（Asia/Shanghai）
> 阶段结论：**G0 PASS**
> 产品上线结论：**NO-GO**；G0 只解除客户端开发的测试隔离前置阻塞，不代表 Windows/Android 客户端已完成或可发布。

## 1. 验收范围

本次验收覆盖仓库治理、破坏性数据库操作熔断、HTTP 目标守卫、一次性 Docker 测试拓扑、项目内缓存重定向、CI 唯一入口以及失败后清理。未实现 Windows/Android 业务功能，也未执行签名、商店发布、生产部署或生产数据访问。

Docker 边界如下：

- 用户已授权现有 `clipshare` 容器可用，但 G0 为避免清表夹具接触持久数据，没有连接或挂载 `clipshare_pgdata`；
- 其他既有容器、网络、卷和镜像仅在运行前后做只读清单比对，不启动、不停止、不删除；
- 测试只创建随机命名的 `clipshare-g0-*` 资源，不执行 `docker system prune`、`docker volume prune`、`docker image prune` 或其他全局清理。

## 2. 最终通过运行

| 模式 | run-id | 命令 | 结果 |
|---|---|---|---|
| Guard | `g0-20260822t193035z-9a6c2acc6fc0` | `scripts/test-sandbox.ps1 -Mode Guard` | PASS，`OVERALL_EXIT_CODE=0` |
| Full | `g0-20260822t193723z-8f8e9e869355` | `scripts/test-sandbox.ps1 -Mode Full` | PASS，`OVERALL_EXIT_CODE=0` |

本机证据保存在被 Git 忽略的 `.sandbox/<run-id>/artifacts/evidence.txt` 与 `.sandbox/<run-id>/logs/*.log`。CI 每次执行 Full 后无论成功或失败均上传对应证据目录；证据不写入用户目录。

两轮验收均针对提交 `b13dfef54964d768d81e803a8308266d4ad52470` 上的同一份未提交工作树执行；证据记录 `SOURCE_DIRTY=true` 和状态摘要。源码清单（排除 `.git` 与 `.sandbox`）在每轮执行前后 SHA-256 均为 `4ecc03307606a6aaac8c75569836e8fcf95b6008d160c9652d300405b3c095d5`，证明构建与测试没有修改被测源码。

## 3. Guard 结果

- Python 守卫单元测试：24 项通过；
- Node 守卫单元测试：通过；
- 正向证明：Python 与 Node 均接受合法 run-id、固定证明文件和 SHA-256 摘要；
- 负向证明：`ENVIRONMENT=development`、非 `_test` 数据库、loopback 或生产/公网数据库主机、数据库 URL query override、缺少证明摘要、loopback 或生产/公网 HTTP URL、错误应用端口均被拒绝，预期退出码为 78；
- Guard 模式不启动 PostgreSQL 或应用服务，不执行数据库/HTTP I/O。

## 4. Full 结果

- PostgreSQL 只存在于无宿主端口的内部网络，数据目录使用限额 `tmpfs`；
- Alembic 从空库升级到当前 head，5 个迁移步骤通过；
- Python 集成测试 session 的自动夹具在任何模块建表、写入或清理前执行 `SELECT current_database(), inet_server_port()`，确认实际连接为本轮 `_test` 数据库和 5432 端口；
- 应用启动前再次执行 Python 守卫，应用与数据库健康检查通过；
- Ruff：全部检查通过；
- mypy：36 个源文件，零问题；
- pytest：176 项通过，总行覆盖率 95%；
- 前端浏览器级冒烟：58 项通过、0 失败；
- `ENC1` 文本/二进制/真实 API 加密 E2E：26 项通过、0 失败；
- PWA manifest、Service Worker 与图标：42 项通过、0 失败；
- Node 运行时实测为 v22.23.2，Node 功能/E2E 合计 126 项通过、0 失败；
- Python、Node、PostgreSQL 和 BuildKit 基础镜像均按 SHA-256 摘要固定；服务与 builder 的 CPU、内存、swap、PID 和 `tmpfs` 上限已生效。

已知非阻塞告警：上游 Starlette `TestClient` 提示未来改用 `httpx2`；本轮 BuildKit 首次 bootstrap 的 inspect 输出一次 Docker Desktop named-pipe context deadline 提示，但命令返回成功，随后资源上限设置、三镜像构建、全部门禁和清理校验均通过。此前 Compose secrets 的 `uid/gid/mode` 兼容性告警已通过改用兼容挂载语法消除。

## 5. 隔离与清理结果

两次最终通过运行均满足：

- 本次标签对应的容器、网络、卷和镜像查询计数为 0；
- 专用 Buildx builder、driver 容器和状态卷不存在；
- 若 BuildKit driver 镜像在运行前不存在，运行后也被精确删除；
- 运行前后既有容器、网络、卷和镜像清单逐项一致；
- 沙盒令牌、运行时目录和各工具缓存已删除；
- 只保留 `.sandbox/<run-id>/logs` 与 `artifacts` 作为审计证据；
- 未持久修改全局/用户环境变量、Docker 配置、Python/npm/.NET/NuGet/Gradle/Android 配置。

G0 实际执行了 Python/npm 工具链隔离验证；.NET/NuGet/MSBuild 与 Gradle/Kotlin/Android 目录已由入口脚本预配置到本轮 `.sandbox`，但因客户端工程尚未创建，其编译、daemon 与平台测试残留验证仍是 W1/A1 的必需门禁。

## 6. 剩余门禁与下一步

当前只能进入 `v0.3-C1`。产品继续保持 NO-GO，至少还需要完成：

1. 冻结 OpenAPI、Problem Details、UTC 与幂等协议；
2. 建立 Web/C#/Kotlin 共用 `ENC1` 测试向量和跨端互操作验证；
3. 生成 Python 的确定性锁定/哈希依赖方案，并纳入依赖审查、SBOM 与来源证明；
4. 后续依次完成 W1、A1、C2、W2/A2 和 R1 的平台、安装、签名、性能、回滚与分批发布门禁。

在 C1 完成前不得开始 Windows/Android 业务 UI；在 R1 全部门禁完成前不得发布客户端。
